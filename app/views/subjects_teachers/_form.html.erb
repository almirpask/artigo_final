<%= form_with(model: subjects_teacher, local: true) do |form| %>
  <% if subjects_teacher.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(subjects_teacher.errors.count, "error") %> prohibited this subjects_teacher from being saved:</h2>

      <ul>
      <% subjects_teacher.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :subject_id %>
    <%= form.text_field :subject_id, id: :subjects_teacher_subject_id %>
  </div>

  <div class="field">
    <%= form.label :teacher_id %>
    <%= form.text_field :teacher_id, id: :subjects_teacher_teacher_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
