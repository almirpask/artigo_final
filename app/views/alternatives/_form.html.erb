<%= form_with(model: alternative, local: true) do |form| %>
  <% if alternative.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(alternative.errors.count, "error") %> prohibited this alternative from being saved:</h2>

      <ul>
      <% alternative.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description, id: :alternative_description %>
  </div>

  <div class="field">
    <%= form.label :answer %>
    <%= form.check_box :answer, id: :alternative_answer %>
  </div>

  <div class="field">
    <%= form.label :questionnaire_id %>
    <%= form.text_field :questionnaire_id, id: :alternative_questionnaire_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
